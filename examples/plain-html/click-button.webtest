test "existing button" {
    browser {
        open "http://127.0.0.1:3000"
        evaluate "document.body.dataset.webtest = 'ready';"
        click id("submit")
        expect text("submitted").visible
    }
}
